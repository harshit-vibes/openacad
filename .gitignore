# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Env / secrets
.env
.env.local
*.env

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp

# Personal Claude/agent local state — do NOT push
.claude/
.remember/

# Per-machine state we keep separate
app/data/state.json
app/data/state.json.lock
app/data/*.sqlite-shm
app/data/*.sqlite-wal
app/data/vaults/*/*.sqlite-shm
app/data/vaults/*/*.sqlite-wal

# Tooling artefacts
app/.coverage
app/uv.lock

# Keep the seeded SQLite + embeddings + migrated markdown vault IN the repo so the app works on clone.
